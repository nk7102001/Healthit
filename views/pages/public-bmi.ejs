<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>BMI Calculator - Healthit</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white text-[#1a1a1a] px-6 py-10">

  <div class="max-w-xl mx-auto">
    <h1 class="text-3xl font-bold text-green-600 mb-6 text-center">📏 Public BMI Calculator</h1>

    <form method="POST" action="/features/bmi" class="space-y-4">
      <div>
        <label class="block mb-1 font-medium">Height (cm)</label>
        <input type="number" name="height" required class="w-full px-4 py-2 border border-gray-300 rounded" />
      </div>

      <div>
        <label class="block mb-1 font-medium">Weight (kg)</label>
        <input type="number" name="weight" required class="w-full px-4 py-2 border border-gray-300 rounded" />
      </div>

      <button type="submit" class="w-full bg-green-600 text-white py-2 rounded hover:bg-green-700 transition">Calculate BMI</button>
    </form>

    <% if (bmi && status) { %>
      <div class="mt-6 text-center">
        <p class="text-xl font-semibold text-green-700">Your BMI: <%= bmi %></p>
        <p class="text-lg text-gray-700">Status: <%= status %></p>
      </div>
    <% } %>

    <div class="mt-6 text-center">
      <a href="/features" class="text-green-600 hover:underline">← Back to Features</a>
    </div>
  </div>
<%- include('../partials/footer') %>
</body>
</html>
